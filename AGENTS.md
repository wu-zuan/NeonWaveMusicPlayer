# NeonWave 專案規則

當使用者說「發布新版本」時，直接完成以下動作：

1. 將目前工作樹的程式碼變更（包含新增檔案）提交並推送到 `origin/main`。
2. 遞增修訂版號，提交並推送版本變更。
3. 建立對應的 `v<版本號>` Git tag 並推送。
4. 用最精簡的文字回報版本號與 Release 連結。

發布流程不需先跑測試、建置、額外驗證，也不需等待 GitHub Actions 或 Release 發布完成；除非使用者另外要求。
