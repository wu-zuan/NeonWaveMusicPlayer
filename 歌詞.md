# NeonWave 歌詞系統運作原理

歌詞引擎位於 `sidecar/lyrics/`,採混合式瀑布搜尋策略,為各種來源的音樂檔案(包含 Metadata 不完整或檔名混亂的檔案)提供同步歌詞。

## 0. YouTube 檔名預處理(`src/utils/youtubeFilename.ts`)

歌詞搜尋時會以純本機規則解析 YouTube 下載檔名,移除 MV/Official/畫質、動態歌詞、引言、Hashtag、節目名稱、OST 與版本標籤。`《歌名》`、`【歌名】`、`Artist - Title` 等結構會轉成乾淨的搜尋候選。解析結果只供歌詞引擎使用,不會改寫音樂檔 Metadata,也不會影響音樂庫的歌名、演出者或前端顯示。

歌詞搜尋會同時使用解析後查詢。無法僅從文字判斷方向的 `A - B` 格式會搜尋兩種排列,再由歌名相似度與音訊時長選出結果。整個流程不呼叫 AI。

## 1. 搜尋流程(`sidecar/lyrics/index.ts`)

### 第 0 階段:本機快取
歌曲旁若已存在同名 `.lrc` 檔即直接使用(內容以原文儲存,顯示時才做簡繁轉換)。

### 第 1 階段:LRCLib 精準比對(快速路徑)
*   使用 `/api/get`,以 `Title + Artist + Duration` 一次請求精準命中。
*   Metadata 乾淨的檔案幾乎都在這一步完成,不再發出其他請求。

### 第 2 階段:多來源並行掃描
若快速路徑未命中,先建立一組**去重後**的查詢字串:

1. 中文核心(只取歌名/歌手中的中文字,高精準)
2. 原始 `Title + Artist`
3. 清洗後的 `Title + Artist`(`text.ts:cleanString` 移除 MV/Official/畫質等雜訊)
4. 檔名綜藝格式解析(`歌手《歌名》…`)
5. `get-artist-title` 檔名解析
6. 清洗後的完整檔名

再將所有查詢同時發給三個來源(`providers.ts`):

| 來源 | 說明 |
|------|------|
| **LRCLib** | 開源同步歌詞庫,`/api/search` |
| **網易雲音樂** | 搜尋 + 歌詞 API,時長 ±15 秒過濾 |
| **酷狗 KuGou** | 三段式(搜尋 → krc 候選 → 下載 LRC),中文歌覆蓋率極高 |

### 第 3 階段:候選評分與選擇
*   `titleScore`:清洗 + 簡體正規化後的標題相似度(完全相同 1.0 / 包含 0.8 / 字元重疊 Jaccard)。
*   `diff`:與本機檔案時長差。≤4 秒視為強訊號(門檻降至 0.15),否則需 ≥0.55。

### 第 4 階段:時間軸校正
選中歌詞與本機檔案時長差在 2~30 秒之間時:
*   已設定 AI:請 AI 平移/伸縮所有時間戳(`ai.ts:callAI`)。
*   未設定 AI:寫入 `[offset:±ms]` 數學位移。

### 第 5 階段:AI 歌詞生成備援
資料庫全部落空且已設定 AI 服務商時,依「檔名 / 音訊標籤 / 兩者」模式組合提示詞請 AI 直接輸出 LRC;回覆 `UNKNOWN:` 或無時間戳則放棄。

支援服務商:OpenAI / Claude / Gemini / OpenRouter / Ollama / Open WebUI(統一由 `ai.ts` 呼叫,可自訂 endpoint 與 model)。

## 2. 前端顯示(`LyricsOverlay.tsx`)

*   彈幕式歌詞:目前行以隨機軌道、大小、配色橫向飄過,直接操作 DOM,零 React 重繪。
*   四種樣式:經典霓虹 / 極簡黑白 / 賽博龐克 / 毛玻璃膠囊(設定頁可切換)。
*   本機音訊校正:讀取前 2MB 音訊偵測實際起音點,自動對齊第一句歌詞。
*   歌詞語言:設定頁可選 簡體 / 繁體 / 原文(OpenCC 轉換,不影響 `.lrc` 快取檔)。
